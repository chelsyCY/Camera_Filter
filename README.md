# Filter Camera using Python

This is a small project using Python function to make a camera with emojis filter for fun 👋🏻 

# Installation
Clone the source locally:

`git clone https://github.com/chelsyCY/Camera_Filter/
cd foco`

Some package that needed to be installed to be run:
`pip install opencv-python
pip install numpy
pip insyall mediapipe`

Buit with **Python**
